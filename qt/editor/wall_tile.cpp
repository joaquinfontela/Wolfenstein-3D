#include "wall_tile.h"
#include <string>
/*
wall_tile::wall_tile(std::string& path, int tipo)
{
    QImage image(path.c_str());
    QPixmap pm = QPixmap::fromImage(image);
    this->label.setPixmap(pm);
    my_image = image;
    this->type = tipo;
}
*/
